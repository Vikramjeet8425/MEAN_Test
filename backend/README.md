# Backend (MEAN CRUD)

## Setup
1. Copy `.env.example` to `.env` and fill values (Mongo URI, MySQL credentials, JWT secret, WEATHER_KEY)
2. Run `npm install`
3. Ensure MongoDB and MySQL are running.
4. Run `node server.js` or `npm run dev`

## Tests
`npm test`
